name: minimal

on:
  workflow_dispatch: {}

jobs:
  t:
    runs-on: self-hosted     # 라벨은 일단 최소화
    steps:
      - name: cmd sanity
        shell: cmd
        run: |
          echo HELLO FROM CMD
          ver
          where powershell
          timeout /t 10
          echo DONE
